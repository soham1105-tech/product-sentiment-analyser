pandas>=2.0.0
numpy>=1.24.0
matplotlib>=3.7.0
seaborn>=0.12.0
nltk>=3.8
tqdm>=4.65.0
scipy>=1.10.0
torch>=2.0.0
transformers>=4.35.0
scikit-learn>=1.3.0
jupyter>=1.0.0
ipykernel>=6.25.0

## 📦 NLTK Resource Downloads

Run once before execution:

```python
import nltk

nltk.download('punkt')
nltk.download('averaged_perceptron_tagger')
nltk.download('maxent_ne_chunker')
nltk.download('words')
nltk.download('vader_lexicon')
```

## 📂 Dataset

The dataset used in this project is the Amazon Fine Food Reviews Dataset.

🔗 Download from Kaggle:  
https://www.kaggle.com/datasets/snap/amazon-fine-food-reviews

After downloading, place the file as:

```bash
/content/Reviews.csv
```

or inside the local project root directory.
